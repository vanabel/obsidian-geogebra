{
	"name": "obsidian-geogebra",
	"version": "0.3.3",
	"description": "Embed interactive GeoGebra (.ggb) files via Electron webview (desktop).",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": ["obsidian", "geogebra", "ggb"],
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.11.0",
		"esbuild": "^0.25.0",
		"obsidian": "^1.7.2",
		"tslib": "^2.8.0",
		"typescript": "^5.7.0"
	}
}
